function update_expiration_warning_flags(
	companyAccount: string,
	shouldUpdate: boolean,
	fn: (error: string, customerErrors: any) => any
) {}

export default {
	update_expiration_warning_flags,
};
